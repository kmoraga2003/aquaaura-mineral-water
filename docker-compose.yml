services:
  web:
    build: .
    container_name: aquaaura_web
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - /app/venv/
    environment:
      - DEBUG=True
      - ALLOWED_HOSTS=*
